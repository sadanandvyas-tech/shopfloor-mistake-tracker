{
  "name": "shopfloor-mistake-tracker",
  "version": "0.1.0",
  "description": "Operations Portal sub-app: log mistakes found on printed products, with browser-side OCR, on-image annotation, and Postgres-backed storage.",
  "main": "server.js",
  "scripts": {
    "start": "node local.js",
    "init-db": "node scripts/init-db.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.5"
  },
  "engines": {
    "node": ">=20"
  }
}
